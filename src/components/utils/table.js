// import { TableControl } from 'react-bootstrap-table-control';


{/* <TableControl
    clickable
      header={[
        { key: "id", name: "#" },
        { key: "name", name: "Name" },
        { key: "currentPrice", name: "Price" }
      ]}
      itens={data?.itemList}
       onClickHeader={(d)=>{console.log("header click",d)}}
        onClickItem={(d)=>{console.log("item click",d)}}
        onClickPage={(d)=>{console.log("page click",d)}}
        page={1}
        sort={{
            column: 'name',
            direction: 'ASC'
        }}
        sortable
        tableProps={{
            striped: true
        }}
        total={data?.itemList?.length}
    /> */}